class Coin { constructor(x, y) { this.x = x; this.y = y; this.life = 500; this.pickedUp = false; sndCoinAppear.play(); } draw() { ctx.fillStyle = "gold"; ctx.fillRect(this.x, this.y, 10, 10); } update() { this.life--; if (this.life <= 0 || this.pickedUp) { for (let i = 0; i < 5; i++) { particles.push(new Particle(this.x, this.y, { x: Math.random() * 10 - 5, y: Math.random() * 10 - 5 }, Math.random() * 5, { r: 255, g: 215, b: 0 }, 100)); } coins.splice(coins.indexOf(this), 1); } } pickUp() { this.pickedUp = true; score += 100; sndCoin.play(); coinCount++; for (let i = 0; i < 5; i++) { particles.push(new Particle(this.x + 5, this.y + 5, { x: Math.random() * 6 - 3, y: Math.random() * 6 - 3 }, Math.random() * 5 + 5, { r: 255, g: 215, b: 0 }, 100)); } } get hitbox() { return new Hitbox(this.x, this.y, 10, 10); } }