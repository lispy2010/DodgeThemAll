class BackgroundEffect { constructor() { this.particles = []; setInterval(() => { for (let i = 0; i < 5; i++) { this.particles.push(new Particle(Math.random() * canvas.width, Math.random() * canvas.height, { x: Math.random() * 2 - 1, y: Math.random() * 2 - 1 }, Math.random() * 5 + 2, { r: 60, g: 60, b: 60 }, 100)); } }, 200); } draw() { for (let i = 0; i < this.particles.length; i++) { this.particles[i].draw(); } } update() { for (let i = 0; i < this.particles.length; i++) { this.particles[i].update(this.particles); } } }